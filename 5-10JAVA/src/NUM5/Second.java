/*Через enum
* `Enum` в Java представляет собой перечисление (набор) констант.
* Оно используется для определения типов данных, которые содержат некоторое ограниченное число объектов.
* `instance` - это переменная экземпляра класса в Java, которая ссылается на объект класса, созданный с использованием конструктора
* */

//из методички
package NUM5;

public enum Second {INSTANCE}
